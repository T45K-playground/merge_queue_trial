name: Wait

on:
  pull_request:
    types: [opened, synchronize]
  merge_group:
    types: [checks_requested]

jobs:
  wait_job:
    runs-on: ubuntu-slim
    steps:
      - name: Wait
        run: |
          sleep 10
